export default function verifyEmail(){
    return <div>
        You should Verify your Email, we send you the verification Link to your Email. <button onClick={() => location.reload()} className="text-blue-600 font-semibold italic">Continuar</button>
    </div>
}